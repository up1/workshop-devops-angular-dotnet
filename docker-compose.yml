services:
  api:
    build:
      context: ./api
    ports:
      - "5001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  web:
    build:
      context: ./web
    ports:
      - "4200:80"
    depends_on:
      - api
